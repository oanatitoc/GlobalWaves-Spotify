package app.pages.CommandNextPrev;

/**
 * Interface for a command, representing an action that can be executed.
 */
public interface Command {
    /**
     * Executes the command.
     */
    void execute();
}
